# Space Shorts Agent dependencies — pinned to locally-verified versions so a
# surprise upstream release can never break the unattended daily run.
groq==1.2.0
python-dotenv==1.2.2
edge-tts==7.2.8
google-auth==2.50.0
google-auth-oauthlib==1.3.1
google-api-python-client==2.195.0
