# Local env / secrets (keep .env.example, never commit real secrets)
.env
.env.*
!.env.example
token.json
client_secrets.json
*.pem
*.key

# Python
venv/
.venv/
__pycache__/
*.pyc

# Node
node_modules/
dist/

# Media outputs & generated artifacts
*.mp4
*.mp3
*.ass
subtitles.vtt
history.json

# Bundled binaries (CI installs ffmpeg via apt; never commit the 197MB exe)
*.exe
ffmpeg.exe

# OS
.DS_Store
